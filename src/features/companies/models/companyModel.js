/**
 * Re-export the shared Company model.
 * All controllers in this feature should import from here
 * or directly from shared/models/company.model.
 */
module.exports = require('../../../shared/models/company.model');